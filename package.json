{
  "name": "latch",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "latch": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test tests/*.test.mjs",
    "check": "pnpm typecheck && pnpm test",
    "latch": "pnpm build && node dist/cli.js",
    "sync:skill": "bash scripts/sync-latch-skill.sh"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
