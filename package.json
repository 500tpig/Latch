{
  "name": "latch",
  "version": "0.6.1",
  "type": "module",
  "bin": {
    "latch": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test tests/*.test.mjs",
    "check:instruction-budget": "node --test --test-name-pattern=\"high-frequency instruction growth requires a reviewed aggregate baseline\" tests/docs-skill.test.mjs",
    "check": "pnpm check:instruction-budget && pnpm typecheck && pnpm test",
    "latch": "pnpm build && node dist/cli.js",
    "skill:link": "bash scripts/link-latch-skill.sh",
    "skill:check": "bash scripts/check-latch-skill.sh"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
